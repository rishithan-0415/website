// Scroll to section smoothly
function scrollToSection(id) {
  document.getElementById(id).scrollIntoView({ behavior: "smooth" });
}

// Show alert
function showAlert() {
  alert("Admissions form coming soon!");
}
